import TauCeti.Foo.TnImportMod

/-!
# A consumer

The criterion pulls back by `TauCeti.Foo.tp_dangler`, which is the #5953 shape: PROSE, so the
build stays green and every resolution-level check passes.

It also cites `TauCeti.Foo.tn_unmoved_kin`, a member of the same namespace that was NOT moved,
and `TauCeti.Foo.tp_danglerXtra`, a LONGER name sharing the moved one's full text.
-/
