public import TauCeti.Ctl.Mod
public import TauCeti.Ctl.Cover

-- Never mentions the module or any of its declarations: the drop is unarguable.
theorem tp_unref_owndecl (n : Nat) : n * 1 = n := Nat.mul_one n
