import TauCeti.Init

namespace TauCeti

/-- PRE-EXISTING: `main` roots one `Submodule` theorem here and keeps four nested. The
`[SAME Submodule]` row is `main`'s own, and no PR that merely opens this file created it. -/
theorem _root_.Submodule.toAddSubgroup_submoduleOf : True := trivial

namespace Submodule

theorem nested_one : True := trivial
theorem nested_two : True := trivial
theorem nested_three : True := trivial
theorem nested_four : True := trivial

end Submodule

namespace LinearEquiv

theorem extendOfIsLattice : True := trivial

end LinearEquiv

end TauCeti
