namespace TauCeti

/-- Directly inside `TauCeti`: rooting must produce `_root_.Wrap.tp_dotted`, unchanged by r600. -/
theorem Wrap.tp_dotted : True := trivial

namespace Wrap

theorem tp_bare : True := trivial

end Wrap

end TauCeti
