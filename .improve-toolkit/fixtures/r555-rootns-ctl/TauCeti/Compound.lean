/-
`namespace TauCeti.Wrap` opens both levels on one line and is just as much the wrapper as a bare
`namespace Wrap`. Three of `AlgHom`'s eleven files looked exactly like this (r561).
-/
namespace TauCeti.Wrap

variable {W : Type*}

/-- A docstring whose continuation line begins with a declaration keyword, which is prose:
structure on its functor of points evaluated at `A`. -/
theorem tp_compound : True := trivial

end TauCeti.Wrap
